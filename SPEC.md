# Alexa Morning News 運用仕様

## 目的と境界

Geminiのみを使い、平日朝の日本語ニュースをAlexaに届ける。APIキー登録、mainへの反映、Pages有効化、Alexaアカウント設定はコードの完成とは別の作業。これらを完了するまでは運用開始としない。

## 構成を見直した理由

- 検索とJSON原稿を一度に要求すると、文字数違反だけでも高価な検索をやり直す。調査と編集を分け、調査の引用・出典IDだけを編集段階へ渡す。
- 自由な5件の文字列では必要分野の脱落を検出できない。15分野の固定フィールドと予定配列を検証し、最終の5項目はプログラムで組み立てる。
- 日付の文字列をモデルに任せると過去・時差違いの予定を混ぜやすい。タイムゾーン付きの日時を検査して読み上げ文へ変換する。
- mainへのpushを配信の前提にすると、保存の競合や保護ルールでニュースまで止まる。公開確認の後にGit保存を行う。
- schedule1回だけでは取りこぼしに弱い。06:25に追加確認し、正しい当日版が公開済みならAPIを使わない。

## API処理

`gemini_client.py` は認証、通信、一時エラー再試行、使用量記録を担当。エラー本文やキーをログへ出さない。通常2リクエスト。モデル内での検索クエリ数はモデルが決めるため、2リクエストという回数は検索料金の上限ではない。

`news_content.py` は調査・編集指示、JSONスキーマ、引用対応、必須分野、予定、Alexa形式を担当。根拠のない分野は未確認として明示する。ただし各5ブロックに最低1件は出典付き情報を必要とし、全体が情報不足の原稿は配信しない。

`generate_news.py` は公開確認→調査→編集→検証→生成物保存を実行。編集の不備には失敗原稿と具体的な検証理由を渡して最大4回修正する。研究結果にない新規事実を補わないよう指示する。コードによる意味の真偽保証はしない。

`publication.py` と `verify_public_feed.py` は公開JSONのHTTPS、Content-Type、サイズ、形式、生成日時、本文ハッシュ、現在の生成物との一致を確認する。

## 日付・重複・再実行

- 基準時刻は処理開始時のUTC。ニュース48時間、予定24時間の範囲を明示する。
- updateDateは基準時刻のUTC、挨拶とUIDの日付は日本時間。遅延時も新しい日付だけを付けた古い原稿は作らない。
- UIDは日本日付・ブロック番号・本文とタイトルのハッシュ。同じ内容を同じ日に再利用する場合はIDを変えない。
- 公開済み判定は、日本日付一致、18時間以内、未来5分より先でない、生成コード+モデル+配信URLの指紋一致、feedとmanifestの整合性が条件。
- 配信前には生成物が当日の6時間以内であることと、公開中の版より古くないことを検査。古い実行の再実行による巻き戻しを防ぐ。Git保存前にも今回の版が公開中か確認する。
- force指定は明示的な再生成。ブランチでのプレビューは公開済み判定を使わない。
- 読み上げの完全重複は拒否するが、意味の近い記事の重複は編集指示と初回確認に依存する。

## 失敗時

| 失敗箇所 | 動作 | 復旧 |
|---|---|---|
| API認証・モデル指定 | 無限リトライせず停止 | Secret・モデル・API設定確認 |
| 429、一時5xx、通信 | リクエスト最大3試行 | 待機要求が長い場合は停止 |
| 検索根拠なし | 調査を1回だけ再試行 | 検索対応・モデル確認 |
| 原稿JSON・文字数・出典ID・日時 | 失敗原稿を同じ根拠で直接再編集 | 5回不合格ならmainで更新失敗案内、healthは失敗 |
| 生成成功・Pages失敗 | 生成artifactを保持 | 失敗ジョブだけ再実行 |
| 公開照合失敗 | ジョブを失敗として報告 | 実URL確認、失敗ジョブ再実行 |
| Git保存失敗 | 公開は確認済みのまま | archiveだけ再実行 |
| 定時実行欠落 | 05:45先行生成、06:00・06:25・06:45回復 | Codex補助監視06:00・06:30・07:00・07:30。Mac/Codex停止時は動かない |

定期実行・配信・Alexaキャッシュには外部サービスの遅延があるため、07:15の更新済み再生を厳密には保証しない。

## 検証と受入条件

オフラインテストでは、調査と編集のAPI分離、編集再試行時に検索が増えないこと、出典ID、必須分野、日時・文字数・重複、失敗時の原稿保持、公開済みスキップ、古い配信の拒否を確認する。架空のテスト原稿は一時ディレクトリのみで生成する。

実運用の受入には、実キーでの生成、出典とニュースの品質確認、公開URLの検証成功、Echo実機で5〜7分程度の再生、平日朝の実更新を別途確認する。テスト成功だけでこれらを完了扱いしない。
